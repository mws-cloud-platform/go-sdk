# json

Пакет для кастомных json типов.

## RawMessageNotNull

Алиас для `json.RawMessage`, который должен быть инициализирован и не должен содержать JSON-значение `null`.

Используется там, где значение `null` недопустимо в json.
